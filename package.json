{
	"name": "metascan",
	"private": true,
	"version": "0.0.0",
	"description": "Fast, resilient TypeScript link-preview engine + HTTP server.",
	"license": "MIT",
	"packageManager": "bun@1.3.14",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"engines": {
		"bun": ">=1.2.0"
	},
	"scripts": {
		"build": "bun run --filter '*' build",
		"test": "bun test",
		"lint": "biome check .",
		"format": "biome format --write .",
		"typecheck": "tsc -p packages/core/tsconfig.json && tsc -p packages/core/tsconfig.test.json && tsc -p apps/server/tsconfig.json"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.5.6",
		"@types/bun": "^1.3.14",
		"typescript": "^7.0.2"
	}
}
